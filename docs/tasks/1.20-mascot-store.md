# Задача 1.20: Создание useMascotStore

**Статус**: ✅ Выполнено

## Описание

Pinia store для управления маскотом-мамонтёнком (слоником). Хранит текущую эмоцию, очередь реплик и флаг видимости. Работает **только на клиенте** (SSR не участвует — маскот живёт внутри страницы урока).

Маскот реагирует на действия пользователя через очередь реплик. Каждая реплика — объект `{ text?, emotion?, delay? }`:

- **По умолчанию** (без `delay`) — реплика ждёт **клика** пользователя для перехода к следующей.
- **С `delay`** — реплика автоматически уходит через N мс.
- Без `text` — пузырь не рисуется (только смена эмоции / пауза).

---

## Связи

| Кто                   | Задача | Роль                                                            |
| --------------------- | ------ | --------------------------------------------------------------- |
| **useGameController** | 1.13   | Пишет в стор: `enqueue()`, `setEmotion()` при событиях урока    |
| **useGameOver**       | 1.14   | Пишет: `setEmotion('sad')`, `enqueue(...)` при Game Over        |
| **Elephant**          | 1.16   | Читает: `currentEmotion`, `currentPhrase`, `isVisible`          |
| **LessonAlert**       | 1.15   | Аватар маскота внутри Alert-подсказки (читает `currentEmotion`) |
| **QuickActions**      | 1.22   | Может вызывать `enqueue()` для контекстных подсказок            |
| **LessonConfig**      | 1.13   | Определяет `mascotPhrases?` для переопределения дефолтных фраз  |

> **Визуальное поведение** (слежение за курсором, позиционирование, анимации) описано в задаче **1.16**. Стор — только данные.

---

## Файлы

```
app/
├── types/
│   └── mascot.ts                          ← MascotEmotion, MascotPhraseInput, MascotPhrase
├── composables/
│   └── mascot/
│       └── useMascotStore.ts              ← Pinia store (defineStore)
└── utils/
    └── constants/
        └── MASCOT_PHRASES.ts              ← дефолтные фразы (переопределяемые в LessonConfig)
```

---

## 1. Типы — `app/types/mascot.ts`

```ts
export type MascotEmotion = 'neutral' | 'happy' | 'worried' | 'sad'

/** Входные данные реплики (без id — генерируется стором). */
export interface MascotPhraseInput {
  /** Текст пузыря. Если не задан — пузырь не рисуется (только смена эмоции / пауза). */
  text?: string
  /** Эмоция маскота. Если задана — сменится когда реплика станет текущей. По умолчанию — текущая. */
  emotion?: MascotEmotion
  /** Авто-dismiss через N мс. Если не задан — ждём клика пользователя. */
  delay?: number
}

/** Реплика в очереди (с id). */
export interface MascotPhrase extends MascotPhraseInput {
  id: string
}
```

---

## 2. Эмоции

Четыре состояния. Визуальная реализация — задача 1.16 (Elephant), здесь только семантика.

| Эмоция    | Когда                                | Визуал (для 1.16)                                                                                                         |
| --------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| `neutral` | По умолчанию, подсказки, ожидание    | Спокойное лицо: дефолтная улыбка (SVG path), круглые зрачки 20px, щёчки `rgba(240,210,142,0.3)`                           |
| `happy`   | Урок пройден, правильное действие    | Сердечки вместо зрачков (SVG 35×35), глаза сдвигаются (left eye x:95.8, right eye x:146.36)                               |
| `worried` | Пользователь близок к ошибке, HP = 1 | «Шок»: SVG-брови (наклонные линии), зигзагообразный рот, разные зрачки (left 15px, right 26.96px), красные щёки `#e34a3e` |
| `sad`     | Провал (Game Over), HP закончились   | «Недовольный»: веки-блоки `#A57CE9` поверх глаз (opacity+translate анимация), та же улыбка SVG но перевёрнутая (грустная) |

> Все переходы анимированы `transition: 1s`. Координаты и размеры — проценты от размера слоника (430.49×288.24 — точка отсчёта). Подробные SVG и координаты — в `docs/mascotinfo.md`.

---

## 3. Реализация стора — `app/composables/mascot/useMascotStore.ts`

```ts
import type { MascotEmotion, MascotPhraseInput, MascotPhrase } from '~/types/mascot'

export const useMascotStore = defineStore('mascot', () => {
  // ── State ──
  const currentEmotion = ref<MascotEmotion>('neutral')
  const phrases = ref<MascotPhrase[]>([])
  const isVisible = ref(true)

  let _dismissTimer: ReturnType<typeof setTimeout> | null = null

  // ── Getters ──

  const currentPhrase = computed<MascotPhrase | null>(() => phrases.value[0] ?? null)
  const hasPhrases = computed(() => phrases.value.length > 0)
  const hasText = computed(() => !!currentPhrase.value?.text)

  // ── Actions ──

  /**
   * Добавить одну или несколько реплик в очередь.
   * Если очередь была пуста — сразу активирует первую.
   */
  function enqueue(items: MascotPhraseInput | MascotPhraseInput[]) {
    const arr = Array.isArray(items) ? items : [items]
    const wasEmpty = phrases.value.length === 0

    for (const input of arr) {
      phrases.value.push({
        ...input,
        id: `phrase-${Date.now()}-${Math.random().toString(36).slice(2, 6)}`
      })
    }

    if (wasEmpty) {
      _activateCurrent()
    }
  }

  /**
   * Убрать текущую реплику. Следующая в очереди становится текущей.
   */
  function dismissPhrase() {
    _clearDismissTimer()
    phrases.value.shift()
    _activateCurrent()
  }

  /**
   * Очистить все реплики.
   */
  function clearPhrases() {
    _clearDismissTimer()
    phrases.value = []
  }

  /**
   * Сменить эмоцию напрямую (без очереди).
   */
  function setEmotion(emotion: MascotEmotion) {
    currentEmotion.value = emotion
  }

  /**
   * Показать/скрыть маскота.
   */
  function setVisible(visible: boolean) {
    isVisible.value = visible
  }

  /**
   * Удобный шортхенд: одна реплика с текстом + эмоция + авто-dismiss.
   */
  function react(text: string, emotion: MascotEmotion, delay = 3000) {
    enqueue({ text, emotion, delay })
  }

  /**
   * Полный сброс (при выходе из урока).
   */
  function $reset() {
    _clearDismissTimer()
    currentEmotion.value = 'neutral'
    phrases.value = []
    isVisible.value = true
  }

  // ── Internal ──

  /**
   * Активировать текущую (первую) реплику:
   * — применить emotion если есть
   * — если нет text и нет delay → сразу перейти к следующей
   * — если есть delay → запустить таймер
   * — если нет delay → ждём клика (ничего не делаем)
   */
  function _activateCurrent() {
    const phrase = phrases.value[0]
    if (!phrase) return

    // Применить эмоцию
    if (phrase.emotion) {
      currentEmotion.value = phrase.emotion
    }

    // Без текста и без delay — мгновенный переход (только смена эмоции)
    if (!phrase.text && !phrase.delay) {
      phrases.value.shift()
      _activateCurrent()
      return
    }

    // Авто-dismiss по таймеру
    if (phrase.delay) {
      _startDismissTimer(phrase.delay)
    }
    // Без delay → ждём клика (dismissPhrase вызовется из компонента)
  }

  function _startDismissTimer(ms: number) {
    _clearDismissTimer()
    _dismissTimer = setTimeout(() => {
      dismissPhrase()
    }, ms)
  }

  function _clearDismissTimer() {
    if (_dismissTimer) {
      clearTimeout(_dismissTimer)
      _dismissTimer = null
    }
  }

  return {
    // state
    currentEmotion,
    phrases,
    isVisible,
    // getters
    currentPhrase,
    hasPhrases,
    hasText,
    // actions
    enqueue,
    dismissPhrase,
    clearPhrases,
    setEmotion,
    setVisible,
    react,
    $reset
  }
})
```

### Почему не `ref` для `_dismissTimer`?

Таймер — служебное значение, не нужен ни в devtools, ни в реактивности. Обычная `let`-переменная. Стор только на клиенте — утечки нет.

### Поведение очереди

```
enqueue([
  { text: 'Привет!', emotion: 'neutral' },          // клик →
  { text: 'Будь осторожен.', emotion: 'worried' },  // клик →
  { emotion: 'neutral' },                           // мгновенно (нет text/delay) →
  { text: 'Начинаем!', delay: 3000 }                // авто-dismiss через 3с
])
```

| Шаг | Текущая реплика        | Эмоция  | Пузырь  | Переход       |
| --- | ---------------------- | ------- | ------- | ------------- |
| 1   | `Привет!`              | neutral | показан | клик          |
| 2   | `Будь осторожен.`      | worried | показан | клик          |
| 3   | `{ emotion: neutral }` | neutral | нет     | мгновенно     |
| 4   | `Начинаем!`            | neutral | показан | авто через 3с |

---

## 4. Дефолтные фразы — `app/utils/constants/MASCOT_PHRASES.ts`

Общие фразы, используемые `gameController`. Урок может переопределить любую через `LessonConfig.mascotPhrases`.

```ts
export const MASCOT_PHRASES = {
  // Приветствия
  greet: 'Привет! Готов к уроку?',
  greetBack: 'Ты вернулся! Давай продолжим.',

  // Подсказки
  hintLink: 'Осторожно с незнакомыми ссылками!',
  hintEmail: 'Проверь адрес отправителя.',
  hintAttachment: 'Не открывай подозрительные вложения!',

  // Результаты
  correct: 'Отлично! Правильное решение!',
  almostCorrect: 'Почти! Но можно лучше.',
  fail: 'Ой, не повезло...',
  failExplain: 'Давай разберём, что пошло не так.',
  complete: 'Поздравляю! Урок пройден!',

  // HP
  hpLow: 'У тебя осталось мало HP, будь внимательнее!',
  hpDepleted: 'HP закончились... Подожди немного.',

  // Общее
  idle: 'Нужна помощь? Кликни на меня.'
} as const

export type MascotPhraseKey = keyof typeof MASCOT_PHRASES
```

### Переопределение в уроке

В `LessonConfig` (задача 1.13) добавить опциональное поле:

```ts
// в app/types/lesson.ts (модификация, задача 1.13)
mascotPhrases?: Partial<Record<MascotPhraseKey, string>>
```

В `gameController` — мерж:

```ts
const phrases = computed(() => ({
  ...MASCOT_PHRASES,
  ...config.mascotPhrases
}))

// Использование:
mascot.react(phrases.value.greet, 'neutral', 4000)
```

---

## 5. Использование

### В gameController (1.13)

```ts
const mascot = useMascotStore()
const phrases = { ...MASCOT_PHRASES, ...config.mascotPhrases }

// Старт урока — цепочка реплик, пользователь кликает «далее»
mascot.enqueue([
  { text: phrases.greet, emotion: 'neutral' },
  { text: 'Сегодня разберёмся с фишингом.' },
  { text: 'Будь внимателен к ссылкам!' }
])

// Правильное действие — авто-dismiss
function onCorrectAction() {
  mascot.react(phrases.correct, 'happy', 3000)
  nextStep()
}

// Подозрительное действие
function onSuspiciousAction() {
  mascot.react(phrases.hintLink, 'worried', 3000)
}

// Провал
function fail(reason: string) {
  mascot.react(phrases.fail, 'sad')
  // ...triggerGameOver
}

// Успех
function complete() {
  mascot.react(phrases.complete, 'happy', 5000)
}
```

### В GameOver (1.14)

```ts
const mascot = useMascotStore()

mascot.enqueue([{ emotion: 'sad' }, { text: 'Не расстраивайся, попробуй ещё раз!', delay: 5000 }])

if (userStore.isDepleted) {
  mascot.enqueue({ text: phrases.hpDepleted, emotion: 'sad' })
}
```

### В Elephant (1.16) — визуальный компонент

```vue
<script setup lang="ts">
const mascot = useMascotStore()
</script>

<template>
  <div v-if="mascot.isVisible" class="elephant">
    <ElephantBody :emotion="mascot.currentEmotion" />
    <ElephantEyes :emotion="mascot.currentEmotion" />

    <ElephantSpeechBubble
      v-if="mascot.hasText"
      :text="mascot.currentPhrase!.text!"
      :auto="!!mascot.currentPhrase!.delay"
      @click="mascot.dismissPhrase()"
    />
  </div>
</template>
```

> Глаза следят за курсором: translate зрачков вычисляется от позиции мыши **без клемпинга** (зрачок может уходить за границу глаза, чтобы следить в любую сторону). Позиция слоника определяется через `getBoundingClientRect()` с пересчётом при `resize` окна.

### Сброс при выходе из урока

```ts
onUnmounted(() => {
  mascot.$reset()
})
```

---

## Чеклист

### 1. Типы

- [ ] 1.20.1 Создать `app/types/mascot.ts`
- [ ] 1.20.2 Тип `MascotEmotion` — `'neutral' | 'happy' | 'worried' | 'sad'`
- [ ] 1.20.3 Интерфейс `MascotPhraseInput` — `{ text?, emotion?, delay? }`
- [ ] 1.20.4 Интерфейс `MascotPhrase extends MascotPhraseInput` — `{ id }`

### 2. Стор

- [ ] 1.20.5 Создать `app/composables/mascot/useMascotStore.ts`
- [ ] 1.20.6 `defineStore('mascot', () => { ... })` — setup-синтаксис
- [ ] 1.20.7 State: `currentEmotion: Ref<MascotEmotion>` — начальное `'neutral'`
- [ ] 1.20.8 State: `phrases: Ref<MascotPhrase[]>` — очередь реплик
- [ ] 1.20.9 State: `isVisible: Ref<boolean>` — видимость маскота, начальное `true`
- [ ] 1.20.10 Getter: `currentPhrase` — `phrases[0] ?? null`
- [ ] 1.20.11 Getter: `hasPhrases` — `phrases.length > 0`
- [ ] 1.20.12 Getter: `hasText` — `!!currentPhrase?.text`
- [ ] 1.20.13 Action: `enqueue(items)` — принимает `MascotPhraseInput | MascotPhraseInput[]`, генерирует `id`, пушит в очередь, если очередь была пуста — вызывает `_activateCurrent()`
- [ ] 1.20.14 Action: `dismissPhrase()` — очистить таймер, `shift()` очередь, вызвать `_activateCurrent()`
- [ ] 1.20.15 Action: `clearPhrases()` — очистить таймер, обнулить массив
- [ ] 1.20.16 Action: `setEmotion(emotion)` — напрямую сменить `currentEmotion`
- [ ] 1.20.17 Action: `setVisible(visible)` — показать/скрыть маскота
- [ ] 1.20.18 Action: `react(text, emotion, delay?)` — шортхенд для `enqueue({ text, emotion, delay })`
- [ ] 1.20.19 Action: `$reset()` — очистить таймер, сбросить emotion в `'neutral'`, очистить `phrases`, `isVisible = true`

### 3. Внутренняя логика очереди

- [ ] 1.20.20 `_activateCurrent()` — при активации реплики: применить `emotion` если задана; если нет `text` и нет `delay` — мгновенный skip (shift + рекурсия); если есть `delay` — запустить `_startDismissTimer(delay)`; если нет `delay` — ждать клика
- [ ] 1.20.21 `_startDismissTimer(ms)` — `setTimeout → dismissPhrase()`, `let`-переменная (не `ref`)
- [ ] 1.20.22 `_clearDismissTimer()` — `clearTimeout` + обнулить переменную

### 4. Дефолтные фразы

- [ ] 1.20.23 Создать `app/utils/constants/MASCOT_PHRASES.ts`
- [ ] 1.20.24 Объект `MASCOT_PHRASES` с ключами: `greet`, `greetBack`, `hintLink`, `hintEmail`, `hintAttachment`, `correct`, `almostCorrect`, `fail`, `failExplain`, `complete`, `hpLow`, `hpDepleted`, `idle`
- [ ] 1.20.25 Тип `MascotPhraseKey = keyof typeof MASCOT_PHRASES`
- [ ] 1.20.26 Пометка для задачи 1.13: добавить `mascotPhrases?: Partial<Record<MascotPhraseKey, string>>` в `LessonConfig`
