# Задача 1.10: Интеграция Nuxt UI в Auth формы

**Статус**: ⬜ Не выполнено

## Описание

Переделать формы логина и регистрации на компоненты Nuxt UI. Auth — это обычные страницы сайта (НЕ OS), поэтому используем стандартные Nuxt UI компоненты без кастомизации.

### Стиль

Белый фон, центрированная карточка:

```
min-h-screen flex items-center justify-center bg-[#f1f1f1]
```

Карточка формы:

```
UCard — дефолтный стиль (bg-white, rounded-lg, shadow)
max-w-md w-full
```

Логотип «Мамонтов.нет» над формой: `text-2xl font-medium`, акцент `.нет` — `text-violet-400`.

## Чеклист

- [ ] 1.10.1 Открыть `pages/app/auth/login/Form.vue`
- [ ] 1.10.2 Обернуть в `UForm` + `UFormField`
- [ ] 1.10.3 `UInput` для email и пароля
- [ ] 1.10.4 `UButton color="primary"` — «Войти»
- [ ] 1.10.5 Валидация через Nuxt UI (required, email format)
- [ ] 1.10.6 Открыть `pages/app/auth/register/Form.vue`
- [ ] 1.10.7 Обернуть в `UForm` + `UFormField`
- [ ] 1.10.8 `UInput` для имени, email, пароля, подтверждения пароля
- [ ] 1.10.9 `UButton color="primary"` — «Зарегистрироваться»
- [ ] 1.10.10 Валидация через Nuxt UI
- [ ] 1.10.11 Ссылка между формами: «Нет аккаунта? Зарегистрируйтесь» / «Уже есть аккаунт? Войдите» — `NuxtLink`
